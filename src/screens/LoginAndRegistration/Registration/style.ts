import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    subtitle: {
        marginTop: 90,
    },
    title: {
        marginTop: 10,
        marginBottom: 65,
    },
    nextButton: {
        marginTop: 45,
    },
});
