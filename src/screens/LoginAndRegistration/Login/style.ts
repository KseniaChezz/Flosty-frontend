import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    enterButton: {
        marginBottom: 60,
    },
    logoContainer: {
        height: 275,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
        paddingTop: 50,
    },
    logoImage: {
        height: 65,
        width: 220,
    },
});
